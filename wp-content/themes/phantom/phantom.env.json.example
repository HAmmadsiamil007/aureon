{
  "$comment": "Phantom environment overrides (ADR-011). Copy to phantom.env.json (git-ignored). Values here override wp_get_environment_type() detection per environment. Never commit real secrets.",
  "env": "production",
  "debug": false,
  "feature": {
    "phantom_core": true,
    "asset_pipeline": false
  },
  "vite": {
    "port": 5173
  }
}
