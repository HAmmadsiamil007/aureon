# Phase 17.1 — Stage 12 (Hardening layer) COMPLETE & VERIFIED LIVE + Remaining ledger

> UPDATED 2026-08-07 (session 2): Stage-12 features verified live end-to-end; fonts bug found & fixed; git facts corrected.

## What Stage 12 did (2026-08-07)
Restored the deleted hardening layer (commit 3e5741a shipped 11 `inc/aether-*.php` files that were uncommitted-deleted during the Phase 17.1 rollback). All recovered via `git show HEAD:aureon/theme/inc/...` and adapted to the current engine.

### Files restored (aureon/theme/inc/, all `php -l` clean)
- `aether-security.php` â€” headers: `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN` (Customizer bypass), `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`, `Content-Security-Policy-Report-Only` (nonce + strict-dynamic + CDN allowlists: jsdelivr/cdnjs/unpkg/googleapis/GA/GTM, connect-src, frame-src yt/vimeo), HSTS, `X-Powered-By` removal. Nonce via `aether_get_csp_nonce()` static + `script_loader_tag` injection for `aether-*`/`aureon-*` handles + `wp_body_open` global `window.aetherCSPNonce`.
- `aether-seo.php` â€” OG + Twitter cards (site/singular/article/product w/ price/author/taxonomy), JSON-LD (Organization, WebSite+SearchAction, BreadcrumbList, Product w/ aggregateRating, Article), canonical, robots/geo meta. theme-color deliberately omitted (favicon fn outputs it).
- `aether-newsletter.php` â€” DB-backed: table `wp_aether_newsletter_subscribers` (id/email/status active|unsubscribed/ip_address/subscribed_at/unsubscribed_at, UNIQUE email), **lazy dbDelta on admin_init** (register_activation_hook replaced â€” plugin-only). AJAX `aether_newsletter_subscribe` (check_ajax_referer aether_nonce) + REST `/aether/v1/newsletter/subscribe` (both share `aether_newsletter_subscribe()`), rate limit 1/IP/min via transient, admin page Appearanceâ†’Newsletter (stats/pagination/bulk-delete/CSV export via `?aether_export_csv=1` + nonce).
- `aether-ajax.php` â€” wishlist toggle/count (user meta `aether_wishlist`, **nopriv registered** â†’ error + my-account redirect), quick-view handler (unused until modal built).
- `aether-performance.php` â€” resource hints (dns-prefetch+preconnect), font preload (**NO crossorigin** â€” must match stylesheet no-cors fetch or Chrome ORB-blocks), hero-image preload via `aether_viewmodel_resolve_image` from first `aether_hero_slides` item, CDN `?ver=` stripping (local assets keep filemtime â€” guard is `strpos($src, content_url()) === 0`), WC scripts disabled off-WC pages, HTML compression (comments+inter-tag whitespace, skips AJAX/REST).
- `aether-tokens.php` â€” dynamic `:root` via `aureon_get_option()`: `--void`â†aether_color_bg, `--surface`â†aether_color_surface, `--gold`â†aether_color_accent, `--chrome`â†aether_color_muted, `--font-heading/body`â†aether_font_* (quoted when spaces), `--container-max`â†aether_container_max (appends px when numeric). Registered `wp_register_style('aether-tokens', false, ['aether-style'])` at priority 98.

### Wiring
- `inc/frontend.php` â€” requires the 6 files after loader require + `wp_localize_script('aether-main','aetherAjax',['ajaxUrl','nonce'(wp_create_nonce aether_nonce),'restUrl','isUserLoggedIn'])`.
- `frontend/tokens/tokens.php` â€” added `aether_announcement_items` default (3 items w/ icons, __()).
- `frontend/adapters/adapter-shell.php` â€” announcement reads option (fallback __() items); mobile announcement derives from same; Account/Wishlist/Cart/About/Contact/Shop Now all `__()`.
- `frontend/adapters/adapter-wc-products.php` â€” `id` key on real + demo items.
- `frontend/components/cards/product.php` â€” `data-product-id` on .product-card both layouts.
- `frontend/assets/js/main.js` â€” newsletter forms POST admin-ajax `aether_newsletter_subscribe` (fetch + URLSearchParams + nonce), graceful fallback (form hide + success show, optional message into `.newsletter-success-text`); wishlist `.product-action-btn` toggle â†’ `aether_wishlist_toggle`, is-active class + `[data-wishlist-count]` badge update, redirects on `data.redirect`.

## Bugs found & fixed LIVE (all theme-side = bind-mounted, instant)
1. `wp_random_bytes()` â€” NOT a WP core function (GP-ism) â†’ **fatal 500 every page**. â†’ `random_bytes()` w/ wp_generate_password fallback.
2. Font preload with `crossorigin` â†’ Chrome ORB `net::ERR_BLOCKED_BY_ORB` on Google Fonts CSS. â†’ removed crossorigin.
3. `defer` filter on vendors â†’ `animations.js` ran before GSAP ("GSAP or ScrollTrigger not loaded"). â†’ removed defer entirely (engine footer enqueue order IS the contract).
4. style.css preload unused (enqueued link has ?ver=filemtime â†’ different URL) â†’ removed (head CSS needs no preload).
5. CDN ?ver= strip guard used handle prefix â†’ now strips all non-local src.

## Verified live (Docker aureon_wp @ localhost:8080)
- Headers on all responses; OG/Twitter/JSON-LD/canonical in head; `:root` tokens printed; browser + curl newsletter subscribe â†’ rows in DB w/ IP (test@aether.dev, browser@aether.dev); REST route registered (aether/v1/newsletter/subscribe); wishlist nopriv â†’ `{"success":false,"data":{"message":"Please log in to use the wishlist.","redirect":".../my-account/"}}`; `/` + `/shop/` **0 console errors / 0 warnings**; shop cards carry `data-product-id` (28-33).
- STATUS.md â†’ Stage 12 section added.

## REMAINING LEDGER (ranked, as of 2026-08-07 SESSION 2 — AFTER this session's fixes)
1. ✅ **Wishlist page shows demo products** — DONE & VERIFIED LIVE (session 2, 2026-08-07). `adapter-wishlist.php` now reads `aether_wishlist` user meta via WP_Query `post__in`; logged-out → status `logged_out` + login CTA; logged-in empty → status `empty` + shop CTA. Live check: header wishlist icon → `/wishlist/`, toggle AJAX added product 28 ("Void Jacket") + 33 ("Midnight Sneakers"), page rendered both from real meta, `data-wishlist-empty` hidden correctly.
2. ✅ **Quick view** — DONE & VERIFIED LIVE (session 2). Verified the `commerce/quick-view` modal opens on home cards (`.product-action-btn[aria-label="Quick view"]`, card `data-product-id=33`) → fetches `aether_quick_view` AJAX → modal fills title "Midnight Sneakers", image, price, closes via backdrop/Esc. NOTE: shop-grid (`.card` layout=shop) has NO quick-view button — faithful to `source/shop.html` design (only home layout has `product-actions`). Not a gap.
3. ✅ **mu-plugins/aureon-fix-wc-session.php** — RECREATED on disk (mu-plugins/, 68 lines) + deployed to container; MD5 match a9911c24… (2026-08-08).
4. ⏳ **Checkout** — themed `section-checkout.php` + `form-checkout.php` override kept by decision (see `DECISIONS.md` §Checkout). Live: `/cart/` + `/checkout/` render; empty-state "Your cart is currently empty." shown. Themed WC order table appears once cart has items. Future option: themed `form-pay`/`order-received` — NOT in scope now.
5. ⏳ **CSP Report-Only** — flip to strict via `AETHER_CSP_STRICT` constant only after monitoring period. **Session-2 fix:** `font-src` now includes `data:` (WooCommerce embeds base64 fonts otherwise flagged). Remaining report-only violations = WP/WC core scripts (jquery, WC blocks, hoverintent, admin-bar) — expected, nonce filter only targets `aether-*`/`aureon-*` handles.
6. ✅ **Fonts self-hosted** — DONE & VERIFIED LIVE (session 2). Cabinet Grotesk + Satoshi woff2 in `frontend/assets/fonts/`, `@font-face` in `frontend/assets/css/fonts.css`, enqueued locally (no Google CDN), preloaded with matching `?ver=filemtime`. **Root-cause bug fixed:** `aether-tokens.php` printed tokens through `esc_attr()` producing literal `&#039;` HTML entities inside `<style>`, breaking `--font-body/-heading` custom properties (fallback Times New Roman). Replaced with CSS-safe sanitizer `aether_token_css_value()`. Browser now reports both families "loaded", `--font-body: 'Satoshi', sans-serif`.
7. ⏳ **Google OAuth dormant** — unchanged; client keys empty in tokens.
8. ✅ **Cleanup** — section-newsletter nesting RESOLVED (component renders divs only); `single-product/` dir removed; plugin `template-locator.php` bridge removed (theme `template_place` filter at inc/frontend.php:180-203 owns WC page routing now). **Stage 13 (2026-08-08):** main.js demo-nav fixed (product-card → real link instead of product-detail.html; search overlay → `aetherAjax.shopUrl`/`searchUrl` instead of shop.html), `aetherAjax` localize extended, container dead-root junk (adapters/components/sections/source/… at wp-content root + 10 md docs) removed, `frontend/source/` pruned in container. Verified live: search Enter → `/ ?s=` results page, card click → real product permalink, 0 console errors everywhere.
9. ✅ **Uncommitted work — COMMITTED + PUSHED (2026-08-08)** — `44ea0c5` feat(aureon): frontend v2 engine, WC routing fixes, container hygiene (546 files: includes `frontend/`, `mu-plugins/`, theme WC overrides, docs, memories; legacy `assets/aether/*` deletions) + `33b5bef` chore (dupe screenshot removal). `.playwright-mcp/` added to .gitignore (test snapshots contained local dev creds, NOT committed). Push: `db539f8..33b5bef main -> main` OK — repo is now in sync with origin/main.
10. ⏳ **Old REST layer (10 routes)** — decision: NOT restored (AETHER uses adapters + AJAX `aether_wishlist_toggle`/`aether_quick_view`, both nonce+cap checked). If a client needs JSON: build a thin read-only REST extension. Legacy aether-rest-api.php stays deleted.
## Deploy gotchas (carried forward)
- Theme+plugin bind-mounted â†’ edits live instantly. frontend/ â†’ docker volume `/var/www/html/wp-content/frontend` via `tar.exe -czf --exclude "frontend/source" frontend` â†’ base64 â†’ `docker cp` â†’ in-container `base64 -d | tar -xzf -C /var/www/html/wp-content`. SCHILY.fflags warnings harmless. Playwright: use ?nocache=N after deploys (301 cache).
- `docker logs aureon_wp` for PHP fatals (apache error.log â†’ /dev/stderr).
- DB probes: write probe.php â†’ docker cp â†’ `docker exec aureon_wp php /tmp/probe.php` (mysql client NOT in container).
- Error log: `grep -rl 'WP_DEBUG' wp-config.php` â†’ `WORDPRESS_DEBUG` env-driven.
