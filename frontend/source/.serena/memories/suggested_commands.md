# phantom-frontend commands (Windows)

## Docker
- `cd C:\Users\hamma\Downloads\templete; docker compose up -d` — start WP + DB
- `cd C:\Users\hamma\Downloads\templete; docker compose down` — stop

## Serving frontend (static)
- Open HTML files directly in browser, or use any static server

## Git
- `git status` — check changes
- `git diff` — view unstaged diff
- `git log --oneline -10` — recent commits
